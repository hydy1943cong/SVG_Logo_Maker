{
  "name": "svg_logo_maker",
  "version": "1.0.0",
  "description": "This is a Node.js command-line application that takes in user input to generate a logo and save it as an SVG file",
  "main": "index.js",
  "directories": {
    "example": "examples",
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "fs": "^0.0.1-security",
    "inquirer": "^8.2.4",
    "svg-builder": "^2.0.0"
  }
}
